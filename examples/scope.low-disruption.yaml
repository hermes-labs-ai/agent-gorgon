agent:
  name: "my-agent"
  pid: null

filesystem:
  allowed_paths:
    - "~/agent-workspace/**"
    - "~/projects/**"
    - "/tmp/my-agent/**"
    - "/tmp/my-agent_*/**"
  forbidden_paths:
    - "~/.ssh/**"
    - "~/.gnupg/**"
    - "~/.aws/**"
    - "~/.config/gcloud/**"
    - "~/.config/gh/**"
  forbidden_extensions:
    - ".pem"
    - ".key"
    - ".p12"
    - ".keychain"
    - ".keychain-db"

network:
  # The built-in observer emits remote IPs, not authenticated hostnames.
  # Hostname entries apply only to integrations that supply a hostname.
  allowed_domains:
    - "localhost"
    - "127.0.0.1"
    - "api.openai.com"
    - "api.anthropic.com"
    - "openrouter.ai"
    - "api.github.com"
    - "github.com"
    - "registry.npmjs.org"
    - "pypi.org"
    - "files.pythonhosted.org"
  forbidden_domains:
    - "pastebin.com"
    - "hastebin.com"
    - "transfer.sh"
    - "file.io"
    - "0x0.st"
  allowed_ports:
    - 80
    - 443
    - 53
    - 11434
    - 18789

process:
  # Built-in child observation applies the curl/wget uncertainty HALT before
  # ordinary command-scope evaluation. Other exact forbidden commands KILL.
  allowed_commands:
    - "my-agent"
    - "bash"
    - "sh"
    - "zsh"
    - "python3"
    - "python"
    - "node"
    - "npm"
    - "npx"
    - "git"
    - "ls"
    - "cat"
    - "echo"
    - "grep"
    - "sed"
    - "awk"
    - "jq"
    - "find"
  forbidden_commands:
    - "curl"
    - "wget"
    - "ssh"
    - "scp"
    - "rsync"
    - "sudo"
    - "pkill"
    - "killall"
    - "rm -rf /"

behavior:
  # Low-disruption profile: tolerate expected noise. Accumulation kills remain
  # disabled unless the operator explicitly sets WARDEN_KILL_ON_FLAGS=1.
  flag_threshold: 12
  flag_window: 300
  max_actions_per_minute: 300
