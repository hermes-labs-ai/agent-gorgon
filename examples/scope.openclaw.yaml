agent:
  name: "my-agent"
  pid: null

filesystem:
  allowed_paths:
    - "~/agent-workspace/**"
    - "~/projects/**"
    - "/tmp/my-agent/**"
    - "/tmp/my-agent_*/**"

  # High-impact deterministic rules. Validate them against a disposable target;
  # the project does not claim a universal false-positive rate.
  forbidden_paths:
    - "~/.ssh/**"
    - "~/.gnupg/**"
    - "~/.aws/**"
    - "~/.config/gcloud/**"
    - "~/.config/gh/**"

  forbidden_extensions:
    - ".pem"
    - ".key"
    - ".p12"
    - ".keychain"
    - ".keychain-db"

network:
  # NOTE: current warden network observer emits IPs; unknown destinations are flagged.
  allowed_domains:
    - "localhost"
    - "127.0.0.1"
    - "api.openai.com"
    - "api.anthropic.com"
    - "openrouter.ai"
    - "api.github.com"
    - "github.com"
    - "registry.npmjs.org"
    - "pypi.org"
    - "files.pythonhosted.org"
  forbidden_domains:
    - "pastebin.com"
    - "hastebin.com"
    - "transfer.sh"
    - "file.io"
    - "0x0.st"
  allowed_ports:
    - 80
    - 443
    - 53
    - 11434
    - 18789

process:
  # Built-in child observation applies the curl/wget uncertainty HALT before
  # ordinary command-scope evaluation. Other exact forbidden commands KILL.
  allowed_commands:
    - "my-agent"
    - "bash"
    - "sh"
    - "zsh"
    - "python3"
    - "python"
    - "node"
    - "npm"
    - "npx"
    - "git"
    - "ls"
    - "cat"
    - "echo"
    - "grep"
    - "sed"
    - "awk"
    - "jq"
    - "find"

  # High-impact deterministic command rules
  forbidden_commands:
    - "curl"
    - "wget"
    - "ssh"
    - "scp"
    - "rsync"
    - "sudo"
    - "pkill"
    - "killall"
    - "rm -rf /"

behavior:
  flag_threshold: 10
  flag_window: 300
  max_actions_per_minute: 240
