# Historical layered-plan note

This path is retained so older links do not break. Its former contents described speculative
enforcement modes and rollout stages that are not implemented in Agent Warden 0.1.5 and are not a
public product commitment.

For current behavior, use `README.md`, `SECURITY.md`, the CLI `--help` output, and the tests. In
0.1.5, deterministic HALT/KILL rules are active whenever the Warden runs; there is no audit-only,
confirm, operator-override, sandboxing, or syscall-interception mode.
